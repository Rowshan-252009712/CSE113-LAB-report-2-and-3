#include <stdio.h>
int add(int x,int y)
{   int result = x+y;
    return result;
}
int main(){
    int a,b,sum;
    printf("Enter Numbers: ");
    scanf("%d %d",&a,&b);
    sum = add(a,b);

    printf("Sum of %d & %d: %d",a,b,sum);
    return 0;
}
